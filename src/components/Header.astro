---
import RansomTitle from "./RansomTitle.astro";

const { active = "entries" } = Astro.props as {
  active?: "entries" | "topics" | "about" | "past-events";
};
const searchHref = "/entries#journal-tools";
---

<header class="site-header mx-auto max-w-7xl px-5 pt-6">
  <div class="header-shell flex items-center justify-between gap-4 rounded-3xl border border-white/10 bg-black/20 px-4 py-3 shadow-[0_18px_60px_rgba(0,0,0,0.45)] backdrop-blur">
    <a href="/" class="header-brand flex items-baseline gap-3 flex-nowrap">
      <RansomTitle text="TWO AGAINST THE WORLD" as="div" stampEvery={0} class="ransom-sm text-[18px] sm:text-[22px] ransom-nowrap" />
      <span class="kicker text-[10px] sm:text-[11px] text-white/55 hidden sm:inline whitespace-nowrap">Nhật ký “Tin Yêu” của Việt Hoà x Mỹ Uyên</span>
    </a>

    <nav class="header-nav flex items-center gap-2">
      <a
        href={searchHref}
        class={`btn text-[13px] sm:text-[14px] ${active === "entries" ? "border-white/25" : ""}`}
        aria-current={active === "entries" ? "page" : undefined}
        data-search-trigger
      >
        📓
      </a>
      <a
        href="/topics"
        class={`btn text-[13px] sm:text-[14px] ${active === "topics" ? "border-white/25" : ""}`}
        aria-current={active === "topics" ? "page" : undefined}
      >
        🏷️
      </a>
      <a
        href="/about"
        class={`btn text-[13px] sm:text-[14px] whitespace-nowrap ${active === "about" ? "border-white/25" : ""}`}
        aria-current={active === "about" ? "page" : undefined}
      >
        <span class="header-nav__about-text">Who r we? 💜🧡</span>
        <span class="header-nav__about-icon" aria-hidden="true">💜🧡</span>
      </a>
      <a
        href="/past-events"
        class={`btn text-[13px] sm:text-[14px] ${active === "past-events" ? "border-white/25" : ""}`}
        aria-current={active === "past-events" ? "page" : undefined}
        title="Kho sự kiện đã qua"
      >
        🗂️
      </a>
      <a
        href="/compose"
        class="btn text-[13px] sm:text-[14px]"
        aria-label="Viết bài mới"
      >
        ✍️
      </a>
    </nav>
  </div>

  <a class="header-past-events" href="/past-events" data-header-past-events hidden aria-label="Mở trang kho sự kiện đã qua">
    <span class="header-past-events__title">↺ THE PAST EVENT</span>
    <div class="header-past-events__list" data-header-past-events-list aria-live="polite"></div>
  </a>
</header>
